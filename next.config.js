// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    // Za lokalne slike NIJE potrebna konfiguracija
    // domains i remotePatterns su samo za eksterne slike
  },
}

module.exports = nextConfig